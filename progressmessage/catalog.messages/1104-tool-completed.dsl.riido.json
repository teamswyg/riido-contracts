{
  "schema_version": "riido-progress-message-dsl-message.v1",
  "message": {
    "code": 1104,
    "key": "tool.completed",
    "usage": "active",
    "category": "tool",
    "description": "A tool/action completed with a short summary. The label argument must be a state-neutral noun phrase and must not include rendered state copy such as collecting/running/completed.",
    "args": [
      {
        "name": "label",
        "type": "string",
        "required": true
      },
      {
        "name": "summary",
        "type": "string",
        "required": true
      }
    ],
    "locales": {
      "ko": "{{label}} 완료 - {{summary}}",
      "en": "{{label}} complete - {{summary}}"
    }
  }
}
