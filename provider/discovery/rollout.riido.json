{
  "schema_version": "riido-provider-discovery-rollout.v1",
  "id": "provider-discovery-policy-rollout",
  "ownership": {
    "contracts": "signed wire shape, bounded roots, lifetime and path safety",
    "control_plane": "authenticated publication, signing, ETag, canary and rollback",
    "daemon": "signature verification, local root resolution, last-known-good cache and executable probes",
    "desktop": "bootstrap PATH and offline fallback only"
  },
  "http": {
    "change_gate": "do not add or change a control-plane route until repository-wide evidence proves that riido-client and riido-client-v2 do not consume it",
    "client_usage_evidence": {
      "checked_consumers": ["riido-client", "riido-client-v2"],
      "checked_commits": {
        "riido-client": "867e4bf482dc4cbc0739fac3c0195212dbd1db40",
        "riido-client-v2": "55d3dca863d9d0c84f48d4f94c8f192b7b752612"
      },
      "result": "no /v1/daemon/agent-bindings or /v1/daemon/runtime-snapshot usage",
      "required_again": "before every daemon route or response change"
    },
    "v1": {
      "method": "GET",
      "path": "/v1/daemon/agent-bindings",
      "compatibility": "immutable and supported during the complete v2 rollout"
    },
    "v2": {
      "method": "GET",
      "path": "/v2/daemon/agent-bindings",
      "request_headers": ["Authorization", "X-Riido-Device-ID", "X-Riido-Provider-Policy-Revision"],
      "response": "v1 bindings plus an optional signed provider_discovery_policy only when the requested revision differs",
      "failure": "daemon retries v1 bindings and keeps an unexpired last-known-good policy, otherwise using daemon-local discovery"
    },
    "forbidden": ["a standalone provider-discovery endpoint", "in-place v1 response changes", "removing v1 before measured v2 adoption and rollback completion"]
  },
  "security": {
    "algorithm": "Ed25519",
    "trust_anchor": "daemon-embedded public key ring",
    "forbidden_payloads": ["absolute paths", "arbitrary executable names", "commands", "arguments", "environment values", "tokens", "customer paths"],
    "maximum_lifetime": "168h",
    "maximum_payload_bytes": 65536
  },
  "cache": {
    "key": ["device profile", "os", "arch"],
    "refresh": "ETag conditional request with bounded backoff",
    "activation": "verify signature, schema, time window and every rule before atomic replacement",
    "rollback": "control-plane republishes a prior signed payload with a new monotonic revision"
  },
  "telemetry": {
    "metrics": {
      "allowed_dimensions": ["provider", "os", "arch", "cache_state", "result_code"],
      "forbidden_dimensions": ["device_id", "revision", "key_id", "resolved path", "error message"],
      "instruments": ["policy_fetch_total", "policy_activation_total", "provider_discovery_total"]
    },
    "traces_and_logs": {
      "emit": ["policy revision change", "cache fallback transition", "first provider failure", "provider recovery"],
      "steady_success_sampling": "at most 1%",
      "errors": "sample the first occurrence, then deduplicate the same provider, stage and error fingerprint for 10 minutes",
      "unknown": "record bounded failure_stage, error_type, retryable and a redacted diagnostic_message of at most 256 bytes outside metric labels"
    },
    "forbidden_payloads": ["raw payload", "signature", "device secret", "resolved path", "candidate relative path", "token", "environment value"],
    "cost_guard": "do not create spans or logs for unchanged five-second binding polls; alert from aggregated metrics and retain detailed evidence only for state changes or sampled failures"
  },
  "rollout": [
    "publish contracts package without runtime consumers",
    "prove daemon-only ownership again, keep v1 unchanged, and add a disabled v2 agent-bindings representation",
    "add daemon v2-to-v1 fallback and shadow policy validation without changing discovery",
    "canary policy activation for one profile with local fallback retained",
    "expand activation after provider smoke and rollback drills",
    "reduce Desktop hardcoded candidates to bootstrap-only in a later Desktop release"
  ],
  "loop": {
    "observation": "Provider search roots are compiled into Desktop and daemon, so vendor install-layout drift can require binary redistribution.",
    "hypothesis": "A signed, bounded root-relative policy can move layout data to the control plane without allowing remote command or absolute-path injection.",
    "execute": "Land the shared contract first, then add disabled publication, daemon shadow evaluation, canary activation and rollback as separate reviewed changes.",
    "evaluate": "Contract tests reject unsafe paths and envelopes; integration tests prove 200/304, signature failure, expiry, cache fallback, canary and rollback behavior.",
    "retrospective": "Only discovery data changes become server-managed; adapter protocol and parser changes still require daemon releases."
  }
}
